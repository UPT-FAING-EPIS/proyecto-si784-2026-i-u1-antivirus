# RustGuard Python dependencies
# Install with: pip install -r requirements.txt

customtkinter>=5.2.2
# maturin is needed only for building the Rust extension — not at runtime
# Install separately: pip install maturin
